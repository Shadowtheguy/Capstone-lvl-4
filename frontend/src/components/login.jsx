import { useState, useRef } from "react";
import { useNavigate } from "react-router-dom";

function LogIn() {
  const navigate = useNavigate();

  return (
    <>
      <h1 className="text-center m-5">W.I.P</h1>
    </>
  );
}

export default LogIn;
